{
  "name": "project",
  "version": "1.3.0",
  "scripts": {
    "lint:check": "eslint .",
    "lint:fix": "eslint --fix",
    "test": "echo 'TODO: Add a test runner and then update this command'",
    "serve": "echo 'TODO: Add your serve command here",
    "start": "npm run serve",
    "build": "mkdir -p build && cp src/*.js build # TODO: Replace with your build command",
    "preversion": "git fetch --tags && npm run lint:check && npm test",
    "version": "npm run build",
    "postversion": "git push --follow-tags"
  },
  "devDependencies": {
    "eslint": "^7.7.0",
    "mongodb": "^4.13.0"
  },
  "engines": {
    "node": ">=14"
  },
  "type": "module",
  "displayName": "Project",
  "description": "Yannai's project",
  "author": "Yannai Fischer ",
  "repository": {
    "type": "git",
    "url": "https://github.com/yannai-fischer/project"
  },
  "dependencies": {
    "babel": "^6.23.0",
    "express": "^4.18.2",
    "lodash": "^4.17.21"
  }
}
